name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print greeting
        run: |
            echo "Hello world"
            echo "Second Log Testing"
            eacho "third testing"
      - name: Print Goodbye
        run: echo "done - bye!"
